## 编码约束
- 保持良好的代码风格、模块结构、代码架构
- 编写完善但不冗长的中文注释
- 优先使用tailwindcss，若tailwind难以实现再用原生css
- 所有代码都应该注重性能表现，要求极致的性能和可维护性
- 除应用名、品牌名、制造商名外，其余UI显示的字符统一经过i18n翻译
- 每个新建代码文件顶部必须添加 `LICENSE.DOC` 中的完整内容作为 license 注释，并按目标文件语言使用对应注释格式

## 视觉约束
- 完全遵守Fluent UI Style Guide，包括布局、组件、动画、图标、交互
- 应优先使用 @fluentui/react-components 中的组件和样式，如果超出该库范围，则应遵守同样的设计规范

## 项目依赖
- Electron ^30.0.1
- React ^18.2.64

## 其他约束
- 使用 pnpm 包管理器
